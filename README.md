## Group Member Contributions

**Idris Zahir**
- Set up the initial project structure and GitHub repository  
- Created the main HTML layout of the application  
- Designed and implemented the CSS styling (pink and grey theme)  
- Linked all files together and prepared the base for JavaScript functionality  
- Started the assignment and established the foundation for the rest of the team  

**Tuguldur Natsagdorj**
-I added an event listener to the Add button that takes the text from the input field and creates a new task. 
-Each task includes a checkbox, the task text, and a delete button.
-For every task, I also added a change event to the checkbox. 
-When the checkbox is checked, the task gets a line-through style and moves to the bottom of the list. 
-If it’s unchecked, the line-through is removed.
-The delete button allows the user to remove a task from the list.  

**Abdulhamid (Tobi)**
- Implemented the delete button event handler to remove tasks from the list
- Improved CSS styling to make the page visually attractive
- Added animations, hover effects, and gradient background
